# pure python sources
python_sources = files([
  'printing.py',
])

# Install pure Python
python.install_sources(
    python_sources,
    pure: false, # will be installed next to binaries
    subdir: korali / 'auxiliar'  # folder relative to site-packages to install to
)
